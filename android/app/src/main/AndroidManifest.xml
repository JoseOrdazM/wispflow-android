<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ironworker_dictation">

    <!-- Permisos para grabación de audio (dictado por voz) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- Permiso para escribir en almacenamiento externo (guardar grabaciones temporales) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    
    <!-- Permiso para leer almacenamiento externo -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- Permiso para acceso a internet (descarga de modelos Whisper si es necesario) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Permiso para estado de red -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permiso para notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Permiso para servicio en primer plano (grabación continua) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Permiso para evitar que el dispositivo entre en modo de suspensión durante la grabación -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="IronWorker Dictation"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/Theme.IronWorkerDictation"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/Theme.IronWorkerDictation"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Servicio en primer plano para grabación de audio -->
        <service
            android:name="com.example.ironworker_dictation.AudioRecordingService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
