name: wispflow_android
description: WispFlow - Floating microphone overlay
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.2.0

dependencies:
  flutter:
    sdk: flutter
  flutter_overlay_window: ^0.4.3
  http: ^1.1.0
  shared_preferences: ^2.2.0
  permission_handler: ^11.1.0
  record: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
